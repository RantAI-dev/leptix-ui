# Toolbar

```toml
[dependencies]
leptix-toolbar = "0.1.0"
```

```rust
use leptix_toolbar::*;
```

See the [Radix UI Toolbar documentation](https://www.radix-ui.com/primitives/docs/components/toolbar) for API details and usage patterns.
