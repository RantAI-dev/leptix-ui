# Select

```toml
[dependencies]
leptix-select = "0.1.0"
```

```rust
use leptix_select::*;
```

See the [Radix UI Select documentation](https://www.radix-ui.com/primitives/docs/components/select) for API details and usage patterns.
